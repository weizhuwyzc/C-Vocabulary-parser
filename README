/*****编译******/
comake2 -UB拉取编译依赖
comake2产出Makefile
make编译

/*****文件说明*****/
conf里是测试样例
output里产出工程和单元测试的可执行文件
dict_parser里存放实现词表解析功能的关键类
其中buildin_data存放整数、浮点数、字符串等基础数据结构类型
user_data里存放用户自定义类型数据结构
如果想要添加自定义的数据类型，可以仿照user_data.*文件定义格式以及解析方法
并且在data_type.h中添加类型名称，以及在dict_parser.cpp的构造函数添加对应的映射关系
test里存放测试文件

/******词表文件格式*******/
第一行为每一列数据的数据类型，第二行开始为需要解析的词表
若某一行词表的列数少于第一行的列数，该行将不进行解析，因为无法得知该行数据应该对应的是哪一列
解析失败或出现错误情况，会将发生异常的文件、函数、行数以及对应输入数据输出到标准错误输出上

